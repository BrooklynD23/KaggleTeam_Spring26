# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001-4q3lxl | pattern | Project framing | Treat the project as a trust-marketplace story: prediction, surfacing, monitoring, onboarding, and accountability | This gives the final report and demo one coherent business-oriented spine instead of five disconnected analyses | Yes — if the team intentionally reframes the showcase | agent |
| D002 | M001-4q3lxl | arch | Website data contract | The local-hosted website will consume exported JSON/CSV/PNG/MD artifacts rather than query analytical storage live | The repo is already artifact-driven, there is no website scaffold yet, and a live backend would add avoidable complexity | Yes — if export-driven local hosting proves too brittle | agent |
| D003 | M001-4q3lxl | scope | Showcase deployment boundary | The showcase is internal academic use only and will run locally rather than on public cloud hosting | This matches the governance boundary and the lack of AWS credits while still supporting team and department demos | No | agent |
| D004 | M001-4q3lxl | arch | Track E modeling role | Track E is delivered as a fairness-audit baseline on an upstream Track A or Track D model, followed by one mitigation lever | This makes Track E answer a stronger question — whether the models work fairly — and avoids inventing a fake standalone predictor | No | agent |
| D005 | M001-4q3lxl | scope | Photo/image work sequencing | True multimodal photo/image work is future work after the core semester milestones; start narrow, then expand only if justified | This preserves the core four-week path while still protecting optional multimodal exploration | Yes — if core milestones land early and cleanly | agent |
| D006 | M001-4q3lxl | operability | Compute ladder | Default to native Windows + RTX 3080 first, then use Colab Pro, then HPC only as conditional fallback | This fits the available resources and keeps heavier setup effort off the critical path until it is needed | Yes — if local modeling quality or runtime proves insufficient | agent |
| D007 | M001-4q3lxl | admin/support | Intern explanation workflow | Planning must explicitly document when to invoke an explainer-style agent for intern-facing process walkthroughs | The repo already contains intern-oriented documentation prior art, and the user wants that workflow formalized as part of execution | Yes | agent |
| D008 | M001-4q3lxl/S01 | architecture | S01 artifact census source of truth | Implement S01 as a standalone reporting command that derives final-summary expectations and checklist data from `scripts/pipeline_dispatcher.py` instead of maintaining a second hardcoded manifest. | The dispatcher already defines approaches, stage order, and required outputs. Reusing that contract prevents drift, keeps orchestration thin, and lets the slice report live repo state honestly while repairing missing final summary markdowns through existing track summary modules. | Yes | agent |
| D009 | M001-4q3lxl / S01 / T01 | architecture | Source the all-track EDA completion checklist from the dispatcher | Expose Track A–E terminal summary metadata through scripts.pipeline_dispatcher.get_eda_summary_contract() derived from PIPELINES and shared approach constants, with Track D's Stage 5 dependency centralized in TRACK_D_SPLIT_DEPENDENCY. | S01 reporting needs one authoritative contract for final summary stages, completion markdowns, canonical launcher commands, and Track D blockers. Deriving the contract from PIPELINES prevents a second manifest from drifting away from the dispatcher and makes contract drift fail loudly in tests and downstream reporting code. | Yes | agent |
| D010 | M001-4q3lxl / S01 / T03 | observability | Status semantics for the EDA completeness reporter | Mark only final summary artifacts regenerated during the current reporter invocation as `repaired`; keep absent upstream stage outputs `missing` even after summary repair, and treat already-present summaries as `existing` on later runs. | The reporter should describe live filesystem truth, not implied completeness. Limiting `repaired` to summaries created in the current run preserves honest census semantics while still surfacing that the command can regenerate terminal markdown handoff artifacts. | Yes | agent |
| D011 | M001-4q3lxl/S01/T02 | architecture | Track B summary regeneration behavior in partial repos | Allow `src.eda.track_b.summary_report` to call `load_snapshot_metadata(..., allow_missing=True)` so the final markdown can emit explicit `missing` placeholders when `data/curated/snapshot_metadata.json` is absent, while keeping strict metadata loading for upstream stage builders. | S01 needs truthful final summaries even in stripped worktrees. Making only the summary path permissive preserves operator visibility about missing snapshot metadata without weakening Track B's upstream stage contracts. | Yes | agent |
| D012 | M001-4q3lxl / S02 | architecture | S02 export packaging architecture | Implement S02 as a standalone `scripts/package_eda_exports.py` command that derives bundle truth from `scripts.pipeline_dispatcher.get_eda_summary_contract()` plus S01 census/checklist outputs and writes an allowlisted bundle under `outputs/exports/eda/`. | The current worktree is intentionally stripped: it lacks `data/` and most upstream analytical artifacts, so packaging cannot depend on curated parquet or pretend missing stages are materialized. A standalone post-EDA packager can honestly expose JSON/CSV/PNG/MD handoff surfaces for downstream reporting and the local website while enforcing governance-safe exclusions and avoiding dispatcher-stage coupling. | Yes | agent |
| D013 | M001-4q3lxl S02 T01 | architecture | Source of truth for EDA export bundle metadata | Derive root/per-track export manifests from scripts.pipeline_dispatcher.get_eda_summary_contract() plus outputs/tables/eda_artifact_census.csv, and copy summary/checklist markdown verbatim without scraping markdown tables. | This keeps bundle status semantics honest, preserves Track D blocker metadata from the dispatcher contract, avoids inventing a second manifest, and lets tests run against tmp repos without requiring curated parquet or live analytical storage. | Yes | agent |
| D014 | M001-4q3lxl / S02 / T02 | governance | How Track E validity-log evidence is represented in the export bundle | Summarize `outputs/logs/track_e_s9_validity_scan.log` into manifest metadata and forbid copying raw `.log` files into `outputs/exports/eda/`. | The slice requires an aggregate-safe, internal-only handoff surface. Carrying the validity outcome forward as metadata preserves the useful signal (`No findings detected.` today) while enforcing the no-raw-storage-copy boundary and making forbidden artifact regressions fail loudly. | Yes | agent |
| D015 | M001-4q3lxl S02 T03 | exports | How synthesized PNG evidence visuals are generated for the EDA export bundle | Generate the overview and per-track PNGs inside `scripts/package_eda_exports.py` from dispatcher contract metadata plus census/blocker totals, and publish their canonical paths in the root and per-track manifests. | The stripped worktree lacks most analytical figures, so copied PNGs would be stale or missing. Deriving packaging visuals from the same contract/census truth as JSON and CSV keeps the handoff truthful, aggregate-safe, and locally reproducible while preserving Track D blocker visibility. | Yes | agent |
| D016 | M001-4q3lxl / S03 | architecture | Feature-plan execution surface for post-M001 work | Store long-lived agent planning under `.gsd/feature-plans/` using seven canonical feature slugs (`track-a-prediction`, `track-b-surfacing`, `track-c-monitoring`, `track-d-onboarding`, `track-e-accountability`, `showcase-system`, `multimodal-experiments`), with each feature plan using `FEATURE_PLAN.md`, `sprints/SPRINT-*/SPRINT.md`, and `phases/PHASE-*.md`. | This keeps milestone drafts authoritative while adding a stable execution layer mapped to real repo seams, current export surfaces, and the trust-marketplace framing. A single naming convention reduces re-scoping for downstream agents and makes the architecture testable with filesystem assertions. | Yes | agent |
| D017 | M001-4q3lxl/S03 | architecture | How S03 feature plans represent current readiness and blockers | Feature-plan lanes must ground to current export manifests/checklists and carry forward missing artifacts or upstream dependencies as explicit blockers instead of implying implementation readiness. | S03 exists to prevent downstream agents from re-scoping or building against imagined surfaces. Keeping blocker truth visible in the planning layer preserves honest execution sequencing for Track A/B missing stage artifacts, Track D's dependency on `outputs/tables/track_a_s5_candidate_splits.parquet`, Track E's upstream-model gate, and showcase export-boundary constraints. | Yes | agent |
| D018 | M001-4q3lxl / S04 | architecture | Canonical filenames for the trust narrative and intern workflow | Place S04-owned narrative content in `.gsd/feature-plans/showcase-system/TRUST_NARRATIVE.md` and `.gsd/feature-plans/showcase-system/INTERN_EXPLAINER_WORKFLOW.md`, then link them from the showcase `FEATURE_PLAN.md`, `SPRINT.md`, and phase doc. | S04 needs dedicated, discoverable docs inside the already-established showcase lane rather than scattered prose or a parallel planning tree. Locking the filenames now gives later agents a stable content contract and makes drift-test coverage straightforward. | Yes | agent |
| D019 | M001-4q3lxl/S05 planning | architecture | S05 milestone handoff verification shape | Implement S05 as a thin integration layer that reuses the existing S01 reporter, S02 packager, and slice pytest contracts, adding only one milestone-level agreement test plus milestone-artifact cleanup checks. | The repo already has authoritative scripts and regressions for the underlying surfaces. Reusing them keeps the final handoff honest, avoids a second orchestration subsystem, and makes stale placeholders or requirement drift fail as part of the same integrated proof. | Yes | agent |
| D020 | M002-c1uww6 planning | architecture | M002 baseline scope and artifact contract | Require Tracks A, B, C, and D1 in M002; keep D2 optional/stretch; defer Track E implementation to M003; standardize outputs under src/modeling/* and outputs/modeling/* with Track A as the default M003 audit target | This preserves the semester-safe path, prevents Track D scope creep, keeps Track C in monitoring framing, and gives downstream audit/reporting work a stable artifact surface instead of ad hoc modeling outputs. | Yes | agent |
| D021 | M003-rdpeu4 roadmap planning | milestone-planning | Primary upstream fairness-audit target selection for M003 | Use Track A baseline model outputs as the default fairness-audit intake target, with Track D as explicit secondary fallback only when Track A intake contract cannot be satisfied. | Project and M002 handoff establish Track A as the default M003 audit path, and this minimizes ambiguity while keeping a bounded fallback. Fixing a primary target early prevents Track E vagueness and keeps audit/mitigation slices focused on one reproducible model surface. | Yes | agent |
| D022 | M003-rdpeu4 roadmap planning | milestone-planning | Evidence gate for stronger-model expansion and compute escalation in M003 | Gate stronger-model adoption on explicit material-improvement thresholds (metric delta plus runtime-cost context), and treat Colab/HPC overflow as conditional-only based on measured local trigger violations. | This prevents model-zoo drift and premature infrastructure drag, keeping M003 aligned with framing-question value and the local-first operating model while still preserving a clear path when local runtime is insufficient. | Yes | agent |
| D023 | M003-rdpeu4 roadmap planning | architecture | How to order M003 delivery slices | Use risk-first ordering with upstream model-intake reproducibility as S01 before any fairness/mitigation/model-comparison work. | M003 fairness and stronger-model claims depend on reproducible upstream scored artifacts in this worktree; proving intake first prevents downstream rework from doc-code drift. | Yes | collaborative |
| D024 | M003-rdpeu4 roadmap planning | modeling | Primary upstream model target for M003 fairness auditing | Audit Track A baseline outputs first as the canonical M003 fairness target, with Track D retained as secondary evidence if needed. | Track A is already established as the default audit intake from M002 closeout and offers strict as-of semantics that align with existing leakage and governance guardrails. | Yes | collaborative |
| D025 | M003-rdpeu4 roadmap planning | operations | Adoption policy for stronger models and overflow compute in M003 | Require an explicit materiality gate for stronger/combined models and keep Colab/HPC escalation conditional on measured runtime/quality triggers. | This keeps R010 focused on framing-question value and prevents premature infrastructure drag under R022 unless local execution is demonstrably insufficient. | Yes | collaborative |
| D026 | M003-rdpeu4 S01 planning | architecture | Canonical artifact contract for M003 upstream fairness-audit intake | Use `outputs/modeling/track_a/audit_intake/` as the single S01 intake bundle root with `scored_intake.parquet`, `manifest.json`, and `validation_report.json`, produced by `python -m src.modeling.track_a.audit_intake` and validated by pytest contract tests. | S02/S03/S04 need one deterministic, machine-readable upstream surface; fixing path/schema/status conventions early prevents downstream ad hoc joins and makes integration failures diagnosable via explicit diagnostics artifacts. | Yes | agent |
| D027 | M003-rdpeu4/S01/T01 | architecture | M003 S01 intake contract key and forbidden-column policy | Define `review_id` as the scored-intake primary key, require `business_id` as the subgroup join key, and enforce forbidden text/demographic column families in the shared validator payload. | Downstream fairness and mitigation slices need deterministic row identity and subgroup linkage while preserving existing no-raw-text/no-demographic-inference guardrails. Centralizing these checks in one contract helper prevents ad hoc schema repair and keeps diagnostics machine-readable. | Yes | agent |
| D028 | M003-rdpeu4/S02 planning | architecture | Canonical runtime boundary and artifact contract for S02 model-aware fairness audit | Implement S02 as `python -m src.modeling.track_e.fairness_audit` that consumes S01 intake from `outputs/modeling/track_a/audit_intake/` and writes a canonical bundle under `outputs/modeling/track_e/fairness_audit/` with `subgroup_metrics.parquet`, `disparity_summary.parquet`, `manifest.json`, and `validation_report.json` using `ready_for_mitigation` / `blocked_upstream` status semantics. | S03 mitigation and S04 comparator slices need one deterministic, machine-readable fairness surface with explicit success/failure diagnostics; separating this runtime from EDA tables prevents model-aware fairness claims from drifting into descriptive-only outputs and preserves fail-closed integration behavior established by S01. | Yes | agent |
| D029 | M003-rdpeu4/S02/T02 | architecture | How fairness runtime handles missing curated business context during subgroup joins | When data/curated/business.parquet is absent or missing intake business IDs, build a synthetic business context from scored intake business_id values with unknown city/category/price attributes and continue subgroup derivation via Track E subgroup utilities. | Slice verification runs in stripped worktrees where review_fact exists but business parquet may be absent; fail-closed gating is only for S01 readiness/schema, so fairness runtime must still produce deterministic aggregate bundle outputs (possibly empty after min_group_size filtering) rather than blocking on optional descriptive context files. | Yes | agent |
| D030 | S02 closeout handoff-contract hardening | M003-rdpeu4/S02/T03 | How S02 handoff tests should treat baseline and split continuity | Require exact payload equality for `baseline_anchor` and `split_context` between S01 intake artifacts and S02 fairness manifest/validation outputs, not just presence of keys. | Downstream mitigation/comparator slices need deterministic continuity to trust that fairness metrics correspond to the same upstream baseline context; strict equality catches subtle drift that key-presence checks miss. | Yes | agent |
| D031 | M003-rdpeu4/S03 planning | modeling | Canonical mitigation lever and status semantics for M003 S03 mitigation bundle | Implement `src.modeling.track_e.mitigation_experiment` using group-wise residual correction fit on non-test splits (train/validation), evaluate on test with clamped predictions, and publish S03 bundle statuses `ready_for_closeout`, `blocked_upstream`, and `blocked_insufficient_signal`. | S03 must provide executable pre/post fairness-vs-accuracy evidence for R009 while avoiding mitigation-theater on tiny/filtered subgroup surfaces. A bounded post-hoc correction lever is reproducible on existing intake artifacts and explicit insufficient-signal blocking prevents vacuous success claims when S02 subgroup/disparity rows are empty. | Yes | agent |
| D032 | M003-rdpeu4/S03/T02 | observability | Mitigation CLI process exit semantics on blocked diagnostic outcomes | `src.modeling.track_e.mitigation_experiment` returns exit code 0 when writing deterministic blocked bundles (`blocked_upstream` or `blocked_insufficient_signal`), and encodes failure state in manifest/validation statuses instead of process failure. | Slice verification and downstream automation consume machine-readable artifacts for both success and blocked states. Returning 0 after writing canonical blocked outputs preserves fail-closed diagnostics without aborting evidence-collection commands that must run regardless of readiness. | Yes | agent |
| D033 | M003-rdpeu4/S03/T03 | operability | Canonical S03 mitigation replay command and artifact root | Treat `python -m src.modeling.track_e.mitigation_experiment --config configs/track_e.yaml --intake-dir outputs/modeling/track_a/audit_intake --fairness-dir outputs/modeling/track_e/fairness_audit --output-dir outputs/modeling/track_e/mitigation_experiment` as the canonical invocation and `outputs/modeling/track_e/mitigation_experiment/` as the only authoritative S03 bundle path. | Locking one command/path removes handoff ambiguity and lets S05/M004 consumers triage status semantics without reconstructing paths or schema assumptions. | Yes | agent |
| D034 | M003-rdpeu4/S03/T01 | architecture | S03 mitigation contract ownership and export surface | Centralize S03 pre/post-delta schema + status-vocabulary validators in `src/modeling/common/mitigation_contract.py` and re-export via `src.modeling.common` for all runtimes/tests. | A single shared contract module prevents runtime/test schema drift and keeps blocked/ready diagnostics deterministic for downstream automation. | Yes | agent |
| D035 | M003-rdpeu4/S04 planning | architecture | Canonical S04 comparator boundary, status semantics, and adoption gate composition | Implement S04 as `python -m src.modeling.track_a.stronger_comparator` consuming S01 intake + S02 fairness bundles plus candidate metrics, writing `outputs/modeling/track_a/stronger_comparator/` with `materiality_table.parquet`, `manifest.json`, and `validation_report.json`; use statuses `ready_for_closeout` and `blocked_upstream`, and encode do-not-adopt outcomes via `adopt_recommendation=false` + decision reasons rather than blocked status. | S04 must close R010 without introducing a new training subsystem in this worktree. A comparator/gate runtime over canonical upstream artifacts keeps scope bounded, preserves continuity with S01/S02 contracts, and ensures fairness/runtimes are explicit adoption constraints rather than optional narrative context. | Yes | agent |
| D036 | M003-rdpeu4/S04/T02 | operability | Process exit semantics for S04 stronger comparator blocked branches | Comparator runtime returns exit code 0 after emitting deterministic blocked_upstream artifacts; automation should key off manifest/validation statuses. | S04 verification and downstream handoffs require machine-readable bundle artifacts on both ready and blocked branches. Returning success when artifacts are emitted avoids losing diagnostics while preserving blocked state via explicit status fields. | Yes | agent |
| D037 | M003-rdpeu4 S04 closeout | M003-rdpeu4/S04/T03 | Comparator handoff continuity across ready and blocked branches | Require `outputs/modeling/track_a/stronger_comparator/{manifest.json,validation_report.json}` to preserve exact `baseline_anchor` and `split_context` payloads from upstream fairness/intake context for both `ready_for_closeout` and `blocked_upstream` outcomes. | S05 closeout and M004 consumers need deterministic provenance so adoption/do-not-adopt decisions are tied to the same upstream baseline/split context. Exact-equality continuity checks catch subtle context drift that key-presence checks miss, especially on blocked branches. | Yes | agent |
| D038 | M003-rdpeu4/S05 planning | modeling-closeout | Canonical S05 closeout boundary and decision semantics | Implement S05 as `src.modeling.m003_closeout_gate` with canonical bundle `outputs/modeling/m003_closeout/`, closeout statuses `ready_for_handoff\ | blocked_upstream`, escalation decisions `local_sufficient\ | overflow_required`, and status interpretation keyed to stage manifest/validation payloads rather than process exit codes. | agent |
| D039 | M003-rdpeu4 S05 T02 | modeling-closeout | How S05 interprets S01→S04 stage outcomes and triggers compute escalation | Closeout status is driven by stage manifest/validation readiness semantics (not subprocess exit codes); `blocked_insufficient_signal` is a soft block that still blocks readiness, comparator `no_fairness_signal` is soft/non-blocking, and escalation is `overflow_required` only on explicit runtime-capacity evidence (`runtime_within_budget=false`, runtime-ceiling breach, OOM/timeout diagnostics) | Stage runtimes intentionally emit blocked-but-artifacts-written branches, so exit codes alone are ambiguous. Using payload semantics preserves deterministic triage and prevents fairness-scarcity branches from being misclassified as compute-capacity overflows. | Yes | agent |
| D040 | M003-rdpeu4/S05/T01 closeout contract hardening | modeling-closeout-contract | Validation semantics for S05 stage block flags and contract diagnostics | Require strict boolean dtype/value validation for `is_hard_block` and `is_soft_block`, and emit deterministic machine-readable reason codes (`missing_columns`, `missing_stages`, `invalid_status`, `invalid_decision`) in closeout contract validators. | S05 closeout automation and downstream handoff tests need stable, parseable diagnostics instead of free-form messages. Strict bool gates prevent silent coercion drift in parquet/json round-trips, and reason codes make triage deterministic across runtime/test/docs surfaces. | Yes | agent |
| D041 | M003-rdpeu4/S06 planning | M003-rdpeu4/S06 planning | S06 fairness status and sufficiency signaling contract | Keep S02 fairness manifest status vocabulary unchanged (`ready_for_mitigation`, `blocked_upstream`) and encode replay sufficiency via a new `signal_sufficiency` diagnostics payload with deterministic outcomes (`primary_sufficient`, `fallback_sufficient`, `insufficient`) plus fallback-strategy metadata. | S03/S04/S05 already gate on existing fairness status values; introducing new status strings would force broad downstream rewiring. A dedicated sufficiency payload preserves compatibility while making empty-signal vs actionable-signal truth explicit and machine-readable for mitigation readiness triage. | Yes | agent |
| D042 | M003-rdpeu4 S06 T02 | modeling/fairness_runtime | How S02 fairness runtime recovers from empty primary subgroup/disparity signal | When primary signal is empty, run deterministic aggregate-safe fallback strategy `split_name_metrics` (grouped by intake split_name) and emit `signal_sufficiency`; keep status vocabulary unchanged (`ready_for_mitigation` on sufficient primary/fallback, otherwise `blocked_upstream`). | Canonical replay had zero primary subgroup/disparity rows due strict min_group_size and sparse upstream context. A split-name fallback guarantees non-empty aggregate signal without exposing row IDs or demographic inference fields, preserves downstream status compatibility, and surfaces deterministic reasons/row deltas for insufficiency triage. | Yes | agent |
| D043 | M003-rdpeu4/S06 T03 closeout | handoff_contract | How S06 handoff should interpret fairness readiness | Define fairness readiness as outcome-aware truth: `ready_for_mitigation` is only valid when `signal_sufficiency.outcome` is `primary_sufficient` or `fallback_sufficient` and subgroup/disparity rows are non-empty; otherwise fail-closed with `blocked_upstream` plus explicit insufficiency reasons. | Downstream S03/S05 and M004 consumers need deterministic semantics that prevent empty-but-ready fairness bundles from being treated as actionable. Encoding this in handoff tests and UAT triage order preserves continuity while surfacing exact insufficiency diagnostics. | Yes | agent |
| D044 | M003-rdpeu4 S07 T01 | modeling/track_e/mitigation | How mitigation should behave when primary test-only alignment is empty on sparse replay surfaces | Run primary test-only evaluation first, then activate a bounded sparse-all-splits fallback (comparison-group filtered) and treat no_correction_groups as non-blocking unless both paths still yield zero comparison rows. | Canonical replay now carries split_name disparities with tiny test support; blocking immediately on no_correction_groups or empty test alignment prevented any contract-valid pre/post evidence. The two-path approach preserves existing status vocabulary, keeps fail-closed behavior when evidence is truly insufficient, and emits machine-readable diagnostics for downstream closeout consumers. | Yes | agent |
| D045 | M003-rdpeu4 S07 T02 | handoff_contract | Sparse-path mitigation diagnostics contract for downstream readiness interpretation | Require `lever_metadata.evaluation_diagnostics` to carry deterministic path fields (`active_path`, `sparse_fallback_activated`, primary/fallback payloads, non-blocking reasons) and treat artifact payloads as source-of-truth for readiness interpretation. | S07 introduced bounded sparse fallback; without required diagnostics shape, downstream S05/M004 consumers would have to infer behavior from brittle branch assumptions or process exits. Locking diagnostics in tests/docs preserves continuity and deterministic triage. | Yes | agent |
| D046 | M003-rdpeu4 S07 T03 | modeling-closeout-contract | Multi-surface agreement rule for closeout handoff readiness | Treat `ready_for_handoff` as valid only when all three persisted surfaces agree: `manifest.stage_rollup.readiness_block_stage_ids=[]`, validation `stage_readiness_matrix` reports no readiness blocks, and `stage_status_table.parquet` marks `s03_mitigation` as `ready_for_closeout` with no hard/soft block flags. | Closeout and stage commands can succeed while still writing blocked artifacts on other branches; requiring agreement across manifest, validation, and table prevents false-ready handoffs and gives M004 consumers deterministic acceptance criteria. | Yes | agent |
| D047 | M004-fjc2zy roadmap planning | narrative | Cross-surface story continuity contract | Establish one canonical narrative/evidence source consumed by website, final report, and presentation deck. | R012 can fail through messaging drift even when individual artifacts look polished; a shared claim-to-evidence contract keeps section ordering, conclusions, and citations aligned across all final showcase surfaces. | Yes | agent |
| D048 | M004-fjc2zy roadmap planning | milestone-planning | M004 slice ordering strategy | Order slices by risk as intake integrity → executive story → drill-downs → report/deck continuity → integrated demo hardening. | The highest failure risk is missing or drifting M001–M003 handoff surfaces; proving intake truth first prevents downstream UI/report work from polishing unstable data assumptions while ensuring each later slice depends on a stable contract. | Yes | agent |
| D049 | M004-fjc2zy roadmap planning | architecture | Website runtime data access model for M004 | Use static/export-driven artifact reads with explicit blocked diagnostics and prohibit live runtime analytics queries. | M004 must satisfy local-hosted and governance boundaries while remaining robust in demo conditions; build-time/static artifact consumption aligns with existing contract-first patterns and avoids accidental backend-product creep. | Yes | agent |
| D050 | M004-fjc2zy/S01 planning | architecture | Canonical intake diagnostics surface for M004 S01 | Generate showcase readiness from `configs/showcase.yaml` into `outputs/showcase/intake/manifest.json` and `outputs/showcase/intake/validation_report.json`, and require the homepage shell to render these statuses directly. | S01's highest risk is hidden handoff drift from missing M001–M003 artifacts. A single generated intake bundle gives site/report/deck slices one inspectable truth source and makes blocked states visible in the UI instead of silent placeholders. | Yes | agent |
| D051 | M004-fjc2zy/S01/T02 | observability | How the showcase shell handles missing or unreadable intake manifests at runtime | Return a deterministic fallback intake payload with shellState='intake_unavailable' and reason code 'INTAKE_MANIFEST_UNAVAILABLE' instead of throwing | Preserves homepage availability, makes failure state explicitly inspectable in UI/tests, and keeps diagnostics stable for smoke checks and downstream automation. | Yes | agent |
| D052 | M004-fjc2zy/S01 closeout | observability | S01 homepage readiness verification contract | Use a manifest-driven smoke checker (`scripts/showcase_smoke_check.py`) that asserts rendered homepage diagnostics against the same intake manifest consumed by runtime, including blocked rows (`surface_key`, `path`, `reason`, `requirement_key`). | This keeps UI verification tied to canonical intake truth and fails fast when diagnostics disappear or drift, preventing polished-but-silent blocked states from reaching demos. | Yes | agent |
| D053 | M004-fjc2zy/S02 planning | architecture | How S02 will prevent narrative drift while adding executive story UX to the local showcase runtime | Introduce a generated executive story contract (`outputs/showcase/story/sections.json` + `validation_report.json`) as the only narrative source consumed by the Next.js executive flow, with section-level blocked diagnostics and reduced-motion-safe Motion transitions. | S02 must advance R011 without masking missing upstream evidence. A generated contract keeps website claims bound to real artifact pointers (`surface_key`, `path`, `reason`, `requirement_key`), preserves R013/R022 visibility when inputs are absent, and gives S03/S04 one reusable continuity surface. | Yes | agent |
| D054 | M004-fjc2zy/S02/T01 | architecture | How executive story sections are ordered in generated story artifacts | Enforce an exact canonical section_order in the story contract (`prediction`, `surfacing`, `onboarding`, `monitoring`, `accountability`) and fail generation when config deviates. | R012 continuity and smoke/UI consumers depend on deterministic executive flow ordering; hard validation prevents narrative drift and avoids downstream UI/tests silently rendering reordered sections. | Yes | agent |
| D055 | M004-fjc2zy/S02/T02 | showcase-ui | How the executive runtime should behave when story artifacts are unavailable | Use a deterministic canonical fallback in `load-story` that preserves the five-section executive order and emits explicit blocked evidence diagnostics | This keeps the executive route renderable, preserves observability surfaces (`surface_key`, `path`, `reason`, `requirement_key`) even during upstream outages, and ensures reduced-motion/accessibility checks can still run against stable UI structure. | Yes | agent |
| D056 | M004-fjc2zy/S02 | showcase-architecture | Canonical ordering for executive trust-story sections in S02 artifacts and runtime | Lock executive sections to `prediction`, `surfacing`, `onboarding`, `monitoring`, `accountability` in the story contract builder and consume that order in UI/smoke checks. | A fixed order prevents narrative drift between generated artifacts, runtime rendering, and parity verification while supporting downstream report/deck continuity work. | Yes | agent |
| D057 | M004-fjc2zy/S02/T03 | showcase-observability | Selector strategy for executive smoke parity assertions | Use `data-testid`-scoped field assertions for section/evidence parity checks instead of brittle adjacent-text scraping. | Next.js SSR markup can inject comment/wrapper nodes between labels and values; testid-scoped extraction keeps checks strict to the right field while avoiding false failures from render wrappers. | Yes | agent |
| D058 | M004-fjc2zy/S03 planning | showcase-architecture | How S03 should prevent drift between track drill-down UI and evidence diagnostics | Introduce a dedicated generated track contract artifact (`outputs/showcase/story/tracks.json` + `tracks_validation_report.json`) as the single source for `/tracks` runtime rendering and smoke parity checks. | S03 extends the executive contract into drill-down routes. Without one generated track artifact, routes, tests, and smoke checks can diverge or hardcode assumptions. A shared artifact keeps canonical ordering, requirement-linked evidence pointers, and blocked diagnostics (R009/R010/R022 continuity) synchronized across generator, UI, and verification surfaces. | Yes | agent |
| D059 | M004-fjc2zy S03 T01 | architecture | How track drill-down contracts encode canonical order and blocked evidence diagnostics | Enforce fixed Track A→E order in config+contract parsing and emit blocked evidence rows (instead of dropping missing pointers) with reason codes and requirement keys in both tracks.json and tracks_validation_report.json. | Downstream Next.js routes and smoke parity checks need deterministic ordering and explicit blocked-state visibility for R009/R010/R022 continuity; preserving diagnostic rows with canonical fields prevents silent evidence drift. | Yes | agent |
| D060 | M004-fjc2zy/S03 closeout | showcase-observability | How showcase smoke verification handles absent local runtime server | When the configured `--base-url` is unreachable, `scripts/showcase_smoke_check.py` auto-starts the local Next.js dev server (`npm --prefix showcase run dev`) and retries parity checks, while preserving an opt-out via `--no-auto-start-showcase`. | Slice and gate verification frequently run in non-interactive contexts where no server is pre-launched. Auto-start preserves deterministic contract parity checks and avoids false-negative connection-refused failures unrelated to artifact/UI drift. | Yes | agent |
| D061 | M004-fjc2zy/S04 planning | showcase-architecture | Canonical source and parity gate for S04 report/deck deliverables | Generate report and deck artifacts only from `outputs/showcase/story/sections.json` + `outputs/showcase/story/tracks.json`, and enforce continuity with a machine-readable parity checker that fails on order/evidence/governance drift. | S04's primary risk is cross-surface narrative drift. Binding report/deck generation to the same artifacts consumed by website routes and adding a parity gate keeps R012 continuity deterministic while preserving R009/R010/R013/R022 diagnostic visibility. | Yes | agent |
| D062 | M004-fjc2zy/S04/T01 | architecture | Canonical report/deck generation contract structure | Use a shared `src/showcase/deliverables_contract.py` normalization layer that validates canonical section/track order and renders deterministic markdown tables with required pointer fields and blocked M003 diagnostics. | A single normalization+render path prevents report/deck drift, keeps governance markers and evidence pointers consistent across surfaces, and provides parseable deterministic output for downstream parity checks. | Yes | agent |
| D063 | M004-fjc2zy/S04/T02 | showcase-observability | How S04 parity diagnostics enforce requirement continuity | Validate `requirement_key` continuity per `evidence_key` row (not only global requirement set membership) and emit mismatch payloads in parity diagnostics. | Set-level checks can pass even when a specific evidence row drifts to the wrong requirement; row-level continuity catches silent cross-surface contract corruption and gives deterministic triage signals for report/deck vs website drift. | Yes | agent |
| D064 | M004-fjc2zy/S05 planning | showcase-architecture | How S05 will enforce end-to-end local demo readiness for M004 closeout | Introduce a single fail-closed `run_showcase_demo_gate` command that orchestrates artifact builds, parity checks, frontend verification, hardened runtime smoke checks, and governance/no-live-query assertions, then writes `outputs/showcase/verification/demo_gate_report.json`. | S01–S04 already prove individual contracts, but milestone closure risk remains in manual multi-step execution and ambiguous failure triage. One integrated gate converts those checks into deterministic assembly proof for R011/R012 continuity while explicitly enforcing R013 governance and R022 blocked-diagnostic visibility boundaries. | Yes | agent |
| D065 | M004-fjc2zy S05 T02 | observability | How the integrated demo gate enforces no-live-query and governance boundaries | Implement a dedicated governance_boundary phase that validates canonical governance markers in config/artifacts/deliverables, blocks showcase/app/api/** routes, and scans showcase/app + showcase/lib for disallowed live-query patterns. | This keeps governance enforcement explicit, fail-closed, and machine-auditable in one phase with structured violation payloads inside demo_gate_report.json for fast triage. | Yes | agent |
| D066 | M004-fjc2zy/S05/T01 | showcase-operability | How smoke startup failures and cleanup are handled for auto-started local showcase runtime | Classify readiness outcomes as `ready`/`unreachable`/`http_error`/`timeout` and manage auto-started `npm --prefix showcase run dev` using process-group-scoped teardown. | Integrated gate triage was ambiguous when startup failures collapsed into one message and leaked child dev-server processes between runs. Explicit readiness classes plus process-group cleanup keep failures actionable and repeated smoke/gate runs deterministic. | Yes | agent |
