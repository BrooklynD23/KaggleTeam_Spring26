# Knowledge Log

- 2026-03-21 — `src.common.artifacts.load_candidate_splits()` can legitimately return configured `t1`/`t2` placeholders with `source == "config"` when `track_a_s5_candidate_splits.parquet` is absent. Summary/report code must check the returned source before describing Stage 5 as materialized; otherwise partial-repo reports falsely imply that Track A Stage 5 exists.
- 2026-03-21 — When interpreting `scripts/report_eda_completeness.py`, treat `repaired` as “final summary regenerated during this invocation only.” A steady-state rerun should leave already-present summary markdowns as `existing` and continue to mark absent upstream stage artifacts as `missing`.
- 2026-03-21 — `scripts/package_eda_exports.py` now wipes `outputs/exports/eda/` before rebuilding it. That reset is intentional: it prevents stale `.parquet` / `.ndjson` / `.log` leftovers from surviving later governance checks and making the bundle look safer than it really is.
- 2026-03-21 — Do not run multiple `scripts/package_eda_exports.py` invocations concurrently against the same worktree. The packager intentionally deletes and recreates `outputs/exports/eda/` on each run, so parallel verification commands race on the bundle root and can fail with transient `FileNotFoundError`s even when the implementation is correct.
- 2026-03-21 — In this harness, `python -m pytest tests/test_package_eda_exports.py` can resolve against the repo mirror under `/mnt/c/...` and report `file or directory not found` even though the test exists in the worktree. Use the absolute worktree path to the test file when running the S02 regression suite from auto-mode.
- 2026-03-21 — `.gsd/feature-plans/README.md` is the canonical planning root surface and `tests/test_feature_plan_architecture.py` enforces more than file counts: each lane must keep `Future Target Folders (Future)` plus repo-path, export-evidence, milestone, and blocker references. When S03 planning tests fail, run the per-slug inventory command from `S03-PLAN.md` first to see whether the break is missing docs, wrong slug/layout, or dropped guardrail language.
- 2026-03-22 — The Track D blocker path is not modeled as a separate artifact row inside `outputs/exports/eda/tracks/track_d/manifest.json`. Future milestone checks should read it from `blocked_by` plus the exported Track D summary / `artifacts.csv`; asserting it appears as its own `artifacts[]` entry will create a false failure.
- 2026-03-22 — `tests/test_trust_narrative_workflow.py` and `tests/test_m001_handoff_verification.py` read `outputs/exports/eda/manifest.json` directly. In a fresh or cleaned worktree, run `python scripts/package_eda_exports.py` before these suites; otherwise they can fail with `FileNotFoundError` even when the docs and requirement state are correct.
- 2026-03-22 — For milestone-close verification, the safe order is `python scripts/report_eda_completeness.py` → `python scripts/package_eda_exports.py` → the integrated pytest suite. Running the packager before refreshing the census/checklist can leave downstream tests asserting against stale completeness truth.
- 2026-03-22 — `S05-UAT.md` plus `outputs/exports/eda/manifest.json` and the per-track manifests are the preferred handoff surfaces for the next milestone. Future agents should start there instead of reconstructing milestone state from slice/task history.
- 2026-03-22 — Treat `.gsd/feature-plans/showcase-system/TRUST_NARRATIVE.md` and `INTERN_EXPLAINER_WORKFLOW.md` as the canonical S04-owned content contract. Showcase/report work should discover them through the showcase feature/sprint/phase docs rather than creating parallel narrative or intern-workflow documents.
- 2026-03-23 — During S06 T03 execution, the slice-level semantic verification script can fail on `R005 not validated` before T04 even when runtime gates are healthy. Treat requirement/roadmap state assertions in that script as T04-owned closure checks, not baseline-runtime regressions.
- 2026-03-23 — In M003-rdpeu4 worktrees, the default `/usr/bin/python3` can lack both `pytest` and `pip`. Use `/mnt/c/Users/Danny/Documents/GitHub/KaggleTeam_Spring26/.venv-local/bin/python` for test and verification commands to avoid false environment failures.
- 2026-03-23 — `python -m src.modeling.track_a.audit_intake` writes `manifest.json` and `validation_report.json` even on blocked upstream input failures; check `manifest.status` (`blocked_upstream`) and `validation_report.phase` (`load_predictions` / `validate_schema`, etc.) before debugging downstream fairness slices.
- 2026-03-23 — In pandas 2.x, assigning a string into a `bool`-typed column raises `TypeError` instead of silently upcasting. When writing malformed-boolean contract tests (e.g., `exceeds_threshold`), cast the column to `object` first so the validator, not DataFrame assignment, owns the failure semantics.
- 2026-03-23 — `src.modeling.track_e.fairness_audit` is designed to succeed (not block) when `data/curated/business.parquet` is missing by synthesizing per-business context from scored intake `business_id` values and assigning unknown subgroup attributes. In stripped worktrees this commonly yields `ready_for_mitigation` with zero subgroup/disparity rows when `subgroups.min_group_size` is high; inspect `manifest.row_counts.dropped_by_min_group_size` before treating empty outputs as runtime failure.
- 2026-03-23 — S02 handoff regressions require `baseline_anchor` and `split_context` in `outputs/modeling/track_e/fairness_audit/{manifest.json,validation_report.json}` to exactly match S01 intake payloads. If fairness runtime rewrites those dicts (key drops/renames/value coercion), `tests/test_m003_fairness_audit_handoff_contract.py` should fail before S03/S04 consume drifted context.
- 2026-03-23 — Slice verification uses `pytest -k "blocked_upstream or insufficient_signal"` for mitigation blocked-branch checks. Keep those exact substrings in blocked mitigation test names, otherwise pytest deselects all tests and exits with code 5 (false-negative verification failure).
- 2026-03-23 — `python -m src.modeling.track_e.mitigation_experiment` intentionally exits with code `0` for `blocked_upstream` and `blocked_insufficient_signal` after writing bundle artifacts; automation must inspect `outputs/modeling/track_e/mitigation_experiment/manifest.json` and `validation_report.json` statuses instead of relying on process exit code alone.
- 2026-03-23 — `python -m src.modeling.track_a.stronger_comparator` mirrors S03 blocked-bundle semantics: it exits 0 after writing deterministic `blocked_upstream` artifacts, so downstream automation must inspect `outputs/modeling/track_a/stronger_comparator/{manifest.json,validation_report.json}` for status/phase and not rely on process exit code alone. Also note that zero-row S02 disparity context is treated as `ready_for_closeout` with `adopt_recommendation=false` (`do_not_adopt_no_fairness_signal`), not as a blocked run.
- 2026-03-23 — `tests/test_m003_comparator_handoff_contract.py` enforces exact continuity for `baseline_anchor` and `split_context` in comparator `manifest.json` and `validation_report.json` for both `ready_for_closeout` and `blocked_upstream`. If blocked-branch code drops or rewrites those payloads, treat it as handoff-contract drift before S05 consumes the bundle.
- 2026-03-23 — In this auto-mode harness, `async_bash` jobs can start from the repo root mirror (`/mnt/c/.../KaggleTeam_Spring26`) instead of the active worktree (`.../.gsd/worktrees/<milestone>`). For milestone-slice verification commands, prefer `bash` (or explicitly pin paths) when worktree-local modules/tests are required; otherwise you can hit false "file/module not found" failures even though files exist in the task worktree.
- 2026-03-24 — `python -m src.modeling.m003_closeout_gate` can exit 0 while still writing a blocked closeout (`manifest.status=blocked_upstream`, often due S03 `blocked_insufficient_signal`). Treat `outputs/modeling/m003_closeout/{manifest.json,validation_report.json}` as source of truth for readiness and escalation, not process exit code.
- 2026-03-24 — S06 fairness sufficiency replay can become `ready_for_mitigation` via `signal_sufficiency.outcome=fallback_sufficient` using fallback `split_name_metrics` even when primary subgroup construction is empty (`primary_*_rows=0`). In this branch, mitigation may still block for alignment/correction scarcity, but `no_disparity_rows` should disappear from mitigation insufficient reasons; use this as a quick regression signal that S02 fallback is active.
- 2026-03-24 — In S03 mitigation, `split_name` disparity rows often produce zero primary test-only comparisons on tiny replay (single test row). Do not treat `no_correction_groups` as an automatic hard block: run sparse fallback evaluation across comparison-group-filtered rows and inspect `lever_metadata.evaluation_diagnostics.active_path` (`primary_test_only`, `sparse_all_splits`, or `none`) to triage whether readiness came from fallback or remained blocked.
- 2026-03-24 — For S07/M003 handoff-readiness verification, require agreement across three closeout surfaces: `manifest.stage_rollup.readiness_block_stage_ids=[]`, validation `stage_readiness_matrix` readiness blocks empty, and `stage_status_table.parquet` row `s03_mitigation` marked `ready_for_closeout` with both block flags false. Any mismatch should be treated as non-ready even if CLI exit codes are 0.
- 2026-03-24 — Milestone closeout can drift if narrative artifacts say a remediation slice is done but the roadmap checkbox remains unchecked (S07 case). Before writing milestone summaries, run `rg "^- \[ \] \*\*S" .gsd/milestones/<MID>/<MID>-ROADMAP.md`; if any slice remains unchecked, fix roadmap state first so DoD verification and state overlays stay consistent.
- 2026-03-24 — In this M004 worktree, `/usr/bin/python3` lacked both `pip` and `pytest`, so slice verification commands using `python -m pytest` failed even though tests were valid. `uv run --with pytest python -m pytest ...` and `uv run --with pyyaml python scripts/build_showcase_intake.py ...` executed successfully without mutating repo files.
- 2026-03-25 — Vitest integration/component suites in `showcase/tests/` can show false failures from cross-test DOM leakage unless `afterEach(cleanup)` is centralized in `showcase/tests/setup.ts`. If homepage/readiness assertions unexpectedly duplicate or mismatch nodes, check global cleanup wiring before debugging component logic.
- 2026-03-25 — `scripts/showcase_smoke_check.py` is manifest-driven: it verifies homepage diagnostics against blocked entries in `outputs/showcase/intake/manifest.json`. Treat smoke failures as UI/contract drift first (missing status labels, blocked rows, or reason text), not server-startup issues, when the app is otherwise reachable.
- 2026-03-25 — `scripts/showcase_smoke_check.py` now auto-starts `npm --prefix showcase run dev` (unless `--no-auto-start-showcase` is passed) when `--base-url` is unreachable. This prevents false-negative `Connection refused` failures in gate runs that call smoke before manually starting the Next.js server.
- 2026-03-25 — When asserting route links in raw Next.js HTML, do not assume attribute order (`data-testid` before/after `href`). Use attribute-order-agnostic matching for `<a ... data-testid="..." ... href="...">` to avoid brittle smoke parity failures.
- 2026-03-25 — In S04 T01, slice-level parity checks intentionally fail until T02 lands because `tests/test_showcase_parity_contract.py` and `scripts/check_showcase_parity.py` are not created yet. Treat missing-parity failures during T01 verification as expected intermediate-state signals, not regressions in report/deck generation.
- 2026-03-25 — Showcase parity continuity must validate `requirement_key` per `evidence_key`, not just the global set of requirement IDs. A drifted row can still leave all expected IDs present elsewhere, so set-only checks miss contract corruption.
- 2026-03-25 — `scripts/showcase_smoke_check.py` readiness failures are now intentionally classed as `unreachable`, `http_error`, or `timeout`; use these labels to triage base-url/network issues vs reachable-but-failing app responses instead of treating all startup failures the same.
- 2026-03-25 — `python scripts/run_showcase_demo_gate.py ...` is the authoritative assembled verification surface for M004 closeout: it can pass with blocked evidence diagnostics (expected in stripped worktrees) as long as parity, smoke, governance markers, and no-live-query boundaries pass; use `outputs/showcase/verification/demo_gate_report.json` `summary.failing_phase_id` plus per-phase failure payloads for first-stop debugging.
- 2026-03-25 — Treat `outputs/showcase/deliverables/parity_report.json` and `outputs/showcase/verification/demo_gate_report.json` as a paired closeout set: parity proves cross-surface story/evidence continuity, while demo gate proves runtime/governance assembly. A passing parity report alone is not sufficient for milestone launchability claims.
- 2026-03-25 — In stripped worktrees, blocked upstream M001/M003 evidence rows are expected and should remain visible across homepage, executive, tracks, report, and deck; success means deterministic blocked diagnostics are preserved end-to-end, not that every upstream artifact is present.
- 2026-03-24 — In this M005 worktree, `python -m pytest ...` fails under `/usr/bin/python3` because `pytest` is unavailable and `pip` is also missing; use `uv run --with pytest python -m pytest ...` for task/slice verification without modifying repo dependencies.
